-# edit.html.haml using partial

%h1 Edit Existing User

= form_tag user_path(@user), :method => :put do

  = label :user_id, :email, 'Email'
  = text_field :user_id, 'email'

  = submit_tag 'Update User Info'